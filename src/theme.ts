import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#FAFBFD',
    },
    primary: {
      main: '#FAFBFD',
    },
    secondary: {
      main: '#0092E1',
    },
  },
  typography: {
    fontFamily: 'Raleway',
  },
});

export default theme;
